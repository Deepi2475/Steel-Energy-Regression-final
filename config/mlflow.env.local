# Local MLflow configuration (no AWS/Neon required)
# Use this file for local development without remote MLflow server
MLFLOW_TRACKING_URI=file:./mlruns

# Optional: Uncomment and set if you want to use a local MLflow server
# MLFLOW_TRACKING_URI=http://localhost:5000

